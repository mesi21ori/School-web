"use client"

import { School } from "@/types/school"

interface FooterSectionProps {
  school: School
}

export function FooterSection({ school }: FooterSectionProps) {
  return <>{/* Footer is handled by the SiteFooter component in the layout */}</>
}
